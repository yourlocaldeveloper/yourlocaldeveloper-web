import React from 'react';

import styles from './ProjectView.module.scss';

export const ProjectView: React.FC = ({}) => {
  return (
    <div className={styles.projectView}>
      <div className={styles.projectViewContainer}></div>
    </div>
  );
};
